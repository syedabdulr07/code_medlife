name: Upload Website

on:
  push:
    branches:
    - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v1

    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: AKIAZTVI7FEFXXESGEOH
        aws-secret-access-key: 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
        aws-region: ap-southeast-2

    - name: Deploy static site to S3 bucket
      run: aws s3 sync ./public/ s3://gitctions --delete